[package]
name = "engine"
version = "0.1.0"
edition.workspace = true

[dependencies]
flow-schema = { path = "../flow-schema" }
automation = { path = "../automation" }
browser-bridge = { path = "../browser-bridge" }
serde_json = "1"
